name: Issue 556 runner probe

on:
  push:
    branches:
      - tmp/issue-556-fix-runner

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo runner-ok
