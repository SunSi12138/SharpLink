# 合法 decimal 校验短路径

堆叠第 3/4 层，固定原dev基线 9a12523852f5545bb741c5650f83d37d2c27685f。

公共 decimal.GetBits 检查保留位与 scale≤28，合法值直接返回，非法值仍走原构造/异常包装。历史默认JIT对v1.1.1：单值解码14.82→13.51ns；256元素数组1500.55→1334.07ns。分配不变，保留负零和scale，不依赖生产native字段偏移；不适用于int Add路径。

每项独立提交，后续层以前层为base。历史微基准来自本对话2026-09-09的原始结果，不是本次新TCP结果，百分比不能相加。保留原来的正确性保护；本次定向验证结果在PR说明中记录，不把栈顶结果当作每层全套CI。SDK覆盖不进入生产提交；无自动合并，#605和owner/完成链不隐含在本栈。
