# 大 bool 集合规范值批量验证

堆叠第 4/4 层，固定原dev基线 9a12523852f5545bb741c5650f83d37d2c27685f。

长度<16保留原逐字节检查；≥16用 ContainsAnyExceptInRange(0,1)，不允许任意非零字节。历史默认JIT对v1.1.1：8元素完整解码33.77→33.75ns；256元素125.45→48.71ns；4096元素1511.94→353.74ns。单元素/8元素纯校验仍有约0.21/0.17ns代价，用户已接受；分配不变，硬件不可用时保留框架回退。

每项独立提交，后续层以前层为base。历史微基准来自本对话2026-09-09的原始结果，不是本次新TCP结果，百分比不能相加。保留原来的正确性保护；本次定向验证结果在PR说明中记录，不把栈顶结果当作每层全套CI。SDK覆盖不进入生产提交；无自动合并，#605和owner/完成链不隐含在本栈。
