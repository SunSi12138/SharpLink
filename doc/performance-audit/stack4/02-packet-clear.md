# 真实组包入口精确清零

堆叠第 2/4 层，固定原dev基线 9a12523852f5545bb741c5650f83d37d2c27685f。

WriteHeaderCore 和 WritePacket 只清零完整15字节帧头；与已合并的 ProtocolV2FrameCodec.BeginFrame 修复为不同入口。历史默认JIT、v1.1.1对照：1KiB组8B包20.26→9.44ns；默认池租用/组包/归还28.08→26.18ns。保留长度占位、帧头初始化、已提交范围、容量和敏感数据清理策略，不承诺TCP同百分比收益。

每项独立提交，后续层以前层为base。历史微基准来自本对话2026-09-09的原始结果，不是本次新TCP结果，百分比不能相加。保留原来的正确性保护；本次定向验证结果在PR说明中记录，不把栈顶结果当作每层全套CI。SDK覆盖不进入生产提交；无自动合并，#605和owner/完成链不隐含在本栈。
