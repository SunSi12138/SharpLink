# 端点随机序号直接映射

堆叠第 1/4 层，固定原dev基线 9a12523852f5545bb741c5650f83d37d2c27685f。

无排除项时，验证参数后直接返回 target；保留排除位图扫描、随机生成器和自定义选路。历史组件证据：8/32/64 端点映射 2.279/7.092/12.639 → 0.582/0.586/0.580 ns；仅为映射内核，不含 RNG、负载选路和 TCP；v1.1.1 无同名新内核，不能伪称这里是其端到端对照。

每项独立提交，后续层以前层为base。历史微基准来自本对话2026-09-09的原始结果，不是本次新TCP结果，百分比不能相加。保留原来的正确性保护；本次定向验证结果在PR说明中记录，不把栈顶结果当作每层全套CI。SDK覆盖不进入生产提交；无自动合并，#605和owner/完成链不隐含在本栈。
