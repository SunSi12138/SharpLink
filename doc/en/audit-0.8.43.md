# SharpLink 0.8.43 deep audit

Version 0.8.43 uses exact 0.8.42 commit `cd2de157` as its baseline and closes one P1 plus four P2 findings: shared-memory creators no longer unlink fresh peer mappings; the receive-credit hot path skips an empty second flow-control lock; implicit connection closure retains its structured error code; early Client response-stream disposal reports abandonment instead of success; and stale dynamic selections cannot retain admission-policy state after generation release.

Pre-fix evidence includes an observed shared-memory initialization failure, concurrent and deterministic fresh-peer probes, deterministic plus 512-way connection-close witnesses, a stopped Client streaming Activity that incorrectly reported success, and a paused old topology selection that recreated state after lifecycle retirement. Every focused post-fix control passes.

The isolated 0.7.11/current investigation localized the first consistent Balanced duplex regression to 0.8.0. Removing only the redundant empty drain produced +6.7%, +9.6%, and +3.7% paired throughput changes (median +6.7%) and -8.8% CPU per stream. Three alternating exact-0.8.42/candidate full-stream pairs changed by +1.5% c2s, -1.8% s2c, +4.0% duplex, and -0.6% for the unary control, with zero failures and no material latency regression. MemoryDiagnoser disproved the earlier process-wide B/item normalization signal, so no allocation regression is claimed.

The final non-incremental Release build has no warnings or errors; Generator is 121/121, Unit 496/496, and Integration 252/252. The 120-second shared-memory Chaos gate completed 812,602 successes, 325,934 expected failures, zero unexpected failures, and 23 restarts, with no Client/Server Errors, 324 ms maximum recovery, a successful drain, and all five final gauges at zero. NativeAOT TCP passed; seven-package pack and fresh-cache TCP/shared-memory functional smoke complete the same final gate. This round found new improvements, so the clean-round counter remains 0/3.
